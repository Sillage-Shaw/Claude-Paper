使用 `arxiv-watch` skill 检索、筛选并总结用户指定领域的最新 arXiv 论文。

用户输入如下：

$ARGUMENTS

执行要求：

1. 严格按照 `.claude/skills/arxiv-watch/SKILL.md` 的流程执行；
2. 所有文件路径、文件创建规则、追加规则和最终回复格式都遵循根目录 `CLAUDE.md`；
3. 如果用户给出了明确路径、时间范围或特殊要求，优先遵循用户要求；
4. 使用 `paper-search` 或可用论文检索工具，不要只凭记忆；
5. 对论文进行去重、相关性筛选和阅读优先级判断；
6. 除非用户明确要求，否则不要下载所有 PDF；
7. 如果用户想定期执行，说明本命令只负责本次 watch，定时触发需要另外设置。
